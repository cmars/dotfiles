#!/bin/bash -ex

TMP_GOPATH=$(mktemp -d)
trap "rm -rf $TMP_GOPATH" EXIT

export GOPATH=$TMP_GOPATH

if [ -n "$1" ]; then
	go get -u $1
	exit $?
fi

for i in $(grep ^[^\#] $HOME/dotfiles/goinstall/packages)
do
	go get -u $i
done
unset i

