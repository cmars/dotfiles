#!/bin/bash -ex

. $HOME/.bash_profile.d/501-gopath-default.bash

if [ -n "$1" ]; then
	go get -u $1
	exit $?
fi

for i in $(grep ^[^\#] $HOME/dotfiles/goinstall/packages)
do
	go get -u $i
done
unset i

